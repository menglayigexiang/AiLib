# AI 协作规则

## 与用户沟通

1. 问题一个一个问。
2. 需要用户选择时，提供编号选项（1、2、3、4、5、6）。
3. 标注推荐选项，并简要说明推荐理由。

## 模块化、可读性与注释硬约束

本节是项目开发的强制规则，不得以开发速度、代码简短、局部性能或“以后再重构”为由规避。

1. 代码必须模块化。按稳定职责划分模块，模块对外只暴露完成职责所需的最小接口；协议、传输、编解码、校验、业务分发、界面和持久化等不同责任不得混杂在同一模块中。
2. 每个类、文件和函数必须有清晰的单一主要职责。出现巨型类、过长函数、过度膨胀文件或多个无关改动原因时，必须按职责拆分，不得继续堆叠分支和状态。
3. 模块依赖方向必须单向、明确并可解释。禁止循环依赖、跨层访问私有实现、为省事绕过已定边界，以及把底层库类型泄漏到无关模块。
4. 抽象必须来自已经存在的职责边界和复用需求。禁止为假设的未来场景提前引入 Factory、Provider、Plugin、Adapter 或多层包装；需要复杂抽象时，必须在设计或注释中说明它解决的具体问题。
5. 代码必须以普通程序员能够逐步阅读、直接审核和安全修改为标准。禁止使用需要猜测执行顺序、隐式副作用、所有权或状态变化的晦涩写法。
6. 手写代码必须具备必要注释。注释说明模块边界、责任、关键约束、非显然决策和错误处理意图；不得只翻译代码表面含义，也不得用大段注释掩盖本应拆分或重命名的难读代码。
7. 新增或修改模块时，测试必须与模块边界对应。优先通过公开行为、稳定内部接口或可控传输替身验证，不得为了测试穿透封装或把私有实现公开化。
8. 自动生成代码和第三方源码不适用本项目的手写注释规则，但必须在目录和构建边界上与项目手写代码明确分离，不得直接修改第三方文件来实现业务逻辑。

## 面向真人审核的代码可读性

代码由真人审核。新增或修改代码时，在不降低正确性、可靠性、必要性能和可维护性的前提下，尽量让人容易看懂、读得舒服；实现方式和代码格式都必须遵守这一原则。

1. 优先选择直接、清晰的实现。避免为了少写几行而使用晦涩技巧、复杂表达式、过深嵌套或不必要的抽象；不能以简短、易读为理由省略必要的校验、错误处理或职责边界。
2. 使用能表达用途的名称，按逻辑组织代码。函数和代码块保持职责清楚，必要时拆分，但不把连贯的简单流程过度拆散。
3. 控制每行的信息量。避免在同一行堆叠多个独立语句；复杂条件、调用和初始化合理换行，使用一致的缩进、空行和括号风格，便于审核者逐步理解。
4. 遵循项目已有格式，并按局部分组对齐中文行尾注释。可读性优先于机械对齐，不为对齐制造过长行或大段无意义空白。
5. 注释补充代码本身难以表达的意图、约束和原因，不只重复代码。必要信息应说清楚，不为追求简短而让审核者猜测。
6. 存在多种质量相当的实现时，优先采用真人审核者更容易理解和验证的方案；确需使用复杂实现时，用简洁注释说明原因及关键约束。

## 中文行尾注释

本规则适用于项目中手写的 C/C++ 头文件、实现文件、示例和测试。后续新增或修改代码时必须遵守：

1. 每个接口声明和函数定义（包括构造函数、析构函数、普通成员函数、自由函数、模板函数和 Lambda）都要添加中文 `//` 行尾注释，说明职责或用途。
2. 每个成员变量、静态变量、全局变量和常量的声明，都要在同一行末尾添加中文 `//` 注释，说明含义；必要时说明单位、默认值或特殊值。
3. 每个局部变量的声明，都要在同一行末尾添加中文 `//` 注释，说明用途；包括循环变量、结构化绑定和条件语句中的变量声明。
4. 函数参数也应说明含义。参数分行时，在各参数所在行末尾添加中文注释；简单单行签名可以在签名行尾注释中说明参数作用。
5. 多行函数签名在签名最后一行添加中文行尾注释；多行变量初始化在变量名所在声明行添加中文行尾注释。不得用仅位于上一行的注释代替行尾注释。
6. 注释应准确说明语义，不只重复名称，不使用英文注释代替要求的中文说明。
7. 修改已有接口或变量时，同步补齐或更新相关中文行尾注释。除非用户明确要求，不因本规则自动改写所有未涉及的历史代码。
8. 自动生成文件、第三方代码和构建产物不适用本规则，不修改它们来补注释。

## 类与结构体注释

1. 每个类（`class`）和结构体（`struct`）的声明、定义都要添加中文注释，包括前向声明、模板类型、嵌套类型和测试类型。
2. 注释使用独立的 `//` 行，紧邻声明上方，不放在类型名所在行末尾。模板类型的注释放在 `template` 声明上方；多行继承声明的注释放在 `class` 或 `struct` 起始行上方。
3. 类注释说明主要职责；结构体注释说明所表达的数据及用途。必要时补充理解该类型所必需的职责边界、关键约束或特殊语义，不只重复类型名称。
4. 在功能描述清楚的前提下言简意赅，优先使用一句话；一句话无法准确说明时，可以使用多行。清楚、准确优先于字数少，不省略必要信息，也不堆砌无关细节。
5. 后续修改已有类型时，同步补齐或更新注释；未涉及的历史代码不自动批量改写。接口、参数和变量继续遵守中文行尾注释及局部对齐规则。

## 行尾注释对齐

1. 同一个声明分组中，尽量让中文行尾注释的第一个文字从同一列开始。使用空格补齐代码与 `//` 之间的间距，统一采用 `// `，不使用 Tab 对齐。
2. 按局部分组对齐：连续的接口声明、成员变量、局部变量，以及同一多行签名中的参数分别成组。空行、访问控制标签或不同代码块可作为分组边界，不要求全文件使用同一列。
3. 通常以组内最长声明为基准，代码与 `//` 之间至少保留两个空格。过长声明可单独处理或合理换行，不为了对齐将整组注释推到过远的位置。
4. 对齐只调整空白，不改变接口、变量名称或代码语义；仍须满足中文行尾注释规则。优先保证在等宽字体下清晰易读。
5. 修改某组声明后，适当调整该组注释的对齐位置，不批量重排未涉及的其他代码。

示例：

```cpp
// 发起协作取消请求，与 Token 共享取消状态，不创建线程。
class CancellationSource;

// 保存服务端报告的 Token 用量，未报告的字段保持未知，区别于零。
struct Usage {
    std::optional<qint64> inputTokens;   // 输入 Token 数，未报告时为未知
    std::optional<qint64> outputTokens;  // 输出 Token 数，未报告时为未知
};

QString id() const;                             // 获取 Agent 实例的稳定身份标识
bool isCancellationRequested() const noexcept;  // 查询调用方是否已请求取消

QString m_id;              // Agent 实例的稳定身份标识
int timeoutSeconds = 120;  // 单次请求超时秒数，-1 表示不限时

const auto calls = response.message.toolCalls();  // 提取本轮响应中完整的工具调用
const auto count = calls.size();                  // 本轮完整工具调用的数量

bool execute(
    const ToolCall& call,  // 本次需要执行的工具调用
    ToolResult& result,   // 输出规范化后的工具业务结果
    SdkError& error);      // 执行工具，返回值表示执行流程是否正常
```

## 验证方式与 Token 使用

通过代码分析、Qt Test、FakeTransport、本地 HTTP 服务和真实协议测试摘要验证功能。为防止 Token 过快消耗，测试和验收过程禁止调用截屏、屏幕录制、实时视频分析或连续图像采样工具。

1. UI 线程、确认等待、取消、生命周期等行为优先用代码测试验证；测试报告记录结果和必要诊断，不逐步截图。
2. UI 验证使用 Qt Test 查找控件、触发交互并断言状态，不使用图像识别、像素对比或视频分析作为验收方式。
3. 用户提供的静态参考图可用于理解需求，但不得在测试阶段主动生成或采集截图进行对比。
